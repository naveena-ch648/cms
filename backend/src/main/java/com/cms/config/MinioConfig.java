package com.cms.config;

import org.springframework.context.annotation.Configuration;

/**
 * MinIO removed. File storage is now handled via PostgreSQL (bytea).
 * @see StorageService
 */
@Configuration
public class MinioConfig {
    // No-op: MinIO replaced by PostgreSQL file storage
}
